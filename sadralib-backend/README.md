# SadraLib Backend (skeleton)

This is a starter skeleton for the SadraLib backend using FastAPI + PostgreSQL.

Steps:
1. Copy `.env` to set environment variables.
2. Build & run with Docker Compose:
   docker-compose up --build -d
3. Create DB tables (simple script or alembic)
4. Start developing.
